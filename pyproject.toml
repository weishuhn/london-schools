[project]
name = "london-schools"
version = "0.1.0"
description = "London schools dashboard with property finder"
requires-python = ">=3.13"
dependencies = [
    "geopy>=2.4.0",
    "playwright>=1.40",
    "requests>=2.31",
]
